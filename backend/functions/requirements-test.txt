# Test-only dependencies — not included in production Docker images
pytest>=8
pytest-mock>=3
httpx>=0.27          # required by FastAPI TestClient

# Code quality & security checks
ruff>=0.9
pip-audit>=2.7
